# Bedrock v0.4.3 性能地基审计

本轮不增加新能力，只处理随着会话、记忆、轨迹和工具增加后可能放大的性能问题。

## 已发现并修复

### 1. 对话区重复全量重绘

旧实现每次 Agent 返回结果都会清空 `Text` 控件，再重新插入整段历史；同一条用户消息会先临时显示，结果返回时又参与整页重建。长会话会出现明显闪烁和主线程停顿。

现在使用稳定消息指纹：

- 数据完全相同：不渲染；
- 原内容是新内容的前缀：只追加新增消息；
- 切换会话、历史窗口发生位移或数据不连续：才全量重建。

### 2. 所有页面在每次结果后同步刷新

旧实现无论用户当前在哪个页面，都会同步查询并重建会话、记忆、技能、轨迹、能力和安全页面。能力页面还可能触发 Windows 注册表应用发现。

现在除对话和会话列表外，其余页面只标记为“数据已过期”；用户切换到该页时才读取。数据指纹没有变化时，不重建控件。

### 3. UI 高频空轮询

旧实现每 100ms 唤醒一次，并在一次回调里无限排空队列。空闲时存在无意义唤醒，事件异常堆积时又可能长时间占据 Tk 主线程。

现在：

- 运行中或有事件：75ms；
- 空闲：350ms；
- 单次最多处理 50 个事件，剩余事件交给下一轮。

### 4. 长会话无限进入模型上下文和桌面

旧实现每一步都会读取整个会话并全部发送给模型，也会在桌面完整渲染。随着时间增长，成本、延迟和内存都会持续上升。

现在默认：

- 模型最近 80 条消息；
- 桌面最近 300 条消息；
- 完整历史仍保存在 SQLite；
- 截断窗口会去掉孤立的工具结果，尽量从用户消息开始。

### 5. 每个工具步骤重复检索记忆和重建工具 schema

旧实现同一任务每调用一次工具都会再次检索长期记忆，并重新构造全部工具 schema。

现在一个执行阶段只检索一次相关记忆；工具 schema 缓存在 `ToolRegistry`，只有注册或替换工具时失效。

### 6. SQLite 每次连接重复设置 WAL

旧实现每次打开数据库连接都执行 `PRAGMA journal_mode=WAL`。现在 WAL 和同步模式只在数据库初始化时设置；普通连接只设置外键和 5 秒 busy timeout。

### 7. 网络客户端与配置重载资源残留

旧实现每次重载模型会创建新客户端，但不会明确关闭旧模型和网页客户端；连接测试也没有关闭临时客户端。

现在 Runtime 支持幂等式清理，模型重载、连接测试和桌面退出都会主动关闭客户端。

### 8. 自动数据无限增长

现在默认仅保留：

- 最近 1000 条自动情景记忆；
- 最近 500 个运行轨迹文件。

用户批准的 lesson 记忆和技能不会被这项清理删除。

### 9. 代码助手一次性构造全部文件列表和全部文本行

代码文件遍历改为生成器；文本搜索按行流式读取，命中上限后立即停止。

## 仍然存在但已隔离的风险

- Tkinter 不是高帧率动画框架；当前界面以稳定和可测试为优先，不做持续动画。
- Python 线程无法强制安全终止正在进行的网络请求，因此依靠模型请求超时控制最长等待。
- 第一次进入能力中心仍可能执行一次 Windows 应用发现；结果缓存 60 秒，手动“重新扫描”会强制刷新。
- 首次打开超大数据库仍需要 SQLite 查询时间，但结果集和界面渲染都有上限。

## 可配置项

```env
AGENT_MODEL_MESSAGE_LIMIT=80
AGENT_DESKTOP_MESSAGE_LIMIT=300
AGENT_MODEL_TIMEOUT_SECONDS=60
AGENT_MODEL_MAX_RETRIES=1
AGENT_TRACE_FILE_LIMIT=500
AGENT_EPISODE_MEMORY_LIMIT=1000
```

不建议为了“看见所有内容”把这些值无限调大。完整历史一直保存在数据库，需要时应做分页或导出，而不是一次性塞进界面或模型上下文。

## v0.4.4 增量约束

- MCP 不在桌面启动阶段自动连接，避免失效的本地进程或远程地址阻塞首屏；
- MCP Server 按需打开、发现/调用后立即关闭，当前版本不保留常驻子进程；
- 对话区对大型工具参数和结果设置显示上限，完整数据仍在 SQLite 与轨迹中；
- 工作区文件复制在后台线程执行，不占用 Tk 主线程；
- 未处理审批从 SQLite 按需读取，不通过高频轮询刷新。
