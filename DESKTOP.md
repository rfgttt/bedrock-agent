# Bedrock Desktop v0.5.1

默认桌面端使用 PySide6 + QML。普通使用不启动 FastAPI，也不监听端口。

## 启动

```powershell
bedrock-desktop
```

首次缺少 DeepSeek API Key 时会显示本机配置对话框。旧版 Tkinter 可通过 `bedrock-desktop-legacy` 回退。

## 交互闭环

- 右上角“帮助与技能”展示可做事项和测试提示词；
- 工具参数在聊天中只显示摘要，点击消息可展开；
- 所有待审批操作进入独立审批中心；
- 工作区页面显示绝对路径，并支持图形化导入；
- 应用页面可添加任意可信 `.exe`/`.lnk`，启动仍需审批；
- Agent 状态栏持续显示当前步骤、工具、风险和 trace。

## 渲染与内存

- 每次只实例化当前页面；
- 列表 delegate 复用；
- 语义数据未变化时不触发模型 reset；
- 对话可匹配前缀时只插入新增行；
- 后台任务使用最多 3 个工作线程；
- 退出时关闭线程池、模型和网络客户端。
